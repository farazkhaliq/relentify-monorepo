# Launch Blockers #3, #4, #6 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete three launch blockers: granular permissions (~40 routes), mismatch flagging (PO-bill + bank-invoice), and developer API + webhooks.

**Architecture:** All three build on existing infrastructure — checkPermission() utility, tier gates, and service layer. Permissions is pure wiring. Mismatches adds one table + one service + two routes + two integration points. Developer API adds auth layer, public routes, webhook delivery, and settings UI.

**Tech Stack:** Next.js 15, PostgreSQL, TypeScript, @relentify/ui components

**Note:** #5 (migration 026 numbering conflict) is already resolved — 026_migration_runs.sql and 027_recording_uploads.sql exist correctly.

---

## Phase 1: Granular Permissions (#3)

### Task 1: Expand WorkspacePermissions type

**Files:**
- Modify: `src/lib/auth.ts` — WorkspacePermissions interface
- Modify: `src/lib/team-defaults.ts` — DEFAULT_PERMISSIONS

- [ ] **Step 1: Expand WorkspacePermissions in auth.ts**

Add new modules to the interface after `customers`:

```typescript
suppliers:   { view: boolean; manage: boolean };
expenses:    { view: boolean; create: boolean; approve: boolean };
quotes:      { view: boolean; create: boolean };
creditNotes: { view: boolean; create: boolean };
journals:    { view: boolean; create: boolean };
po:          { view: boolean; create: boolean; approve: boolean };
projects:    { view: boolean; manage: boolean };
mileage:     { view: boolean; create: boolean; approve: boolean };
vat:         { view: boolean; submit: boolean };
coa:         { view: boolean; manage: boolean };
audit:       { view: boolean };
entities:    { view: boolean; manage: boolean };
```

- [ ] **Step 2: Expand DEFAULT_PERMISSIONS in team-defaults.ts**

All new modules: `view: true`, write actions `false`:

```typescript
suppliers:   { view: true,  manage: false },
expenses:    { view: true,  create: false, approve: false },
quotes:      { view: true,  create: false },
creditNotes: { view: true,  create: false },
journals:    { view: true,  create: false },
po:          { view: true,  create: false, approve: false },
projects:    { view: true,  manage: false },
mileage:     { view: true,  create: false, approve: false },
vat:         { view: true,  submit: false },
coa:         { view: true,  manage: false },
audit:       { view: true },
entities:    { view: true,  manage: false },
```

- [ ] **Step 3: Commit**

### Task 2: Update team page MODULE_CONFIG

**Files:**
- Modify: `app/dashboard/team/page.tsx` — MODULE_CONFIG array

- [ ] **Step 1: Add all new modules to MODULE_CONFIG**

Add after the existing `customers` entry:

```typescript
{ module: 'suppliers',   actions: [{ key: 'view', label: 'View' }, { key: 'manage', label: 'Manage' }] },
{ module: 'expenses',    actions: [{ key: 'view', label: 'View' }, { key: 'create', label: 'Create' }, { key: 'approve', label: 'Approve' }] },
{ module: 'quotes',      actions: [{ key: 'view', label: 'View' }, { key: 'create', label: 'Create' }] },
{ module: 'creditNotes', actions: [{ key: 'view', label: 'View' }, { key: 'create', label: 'Create' }] },
{ module: 'journals',    actions: [{ key: 'view', label: 'View' }, { key: 'create', label: 'Create' }] },
{ module: 'po',          actions: [{ key: 'view', label: 'View' }, { key: 'create', label: 'Create' }, { key: 'approve', label: 'Approve' }] },
{ module: 'projects',    actions: [{ key: 'view', label: 'View' }, { key: 'manage', label: 'Manage' }] },
{ module: 'mileage',     actions: [{ key: 'view', label: 'View' }, { key: 'create', label: 'Create' }, { key: 'approve', label: 'Approve' }] },
{ module: 'vat',         actions: [{ key: 'view', label: 'View' }, { key: 'submit', label: 'Submit' }] },
{ module: 'coa',         actions: [{ key: 'view', label: 'View' }, { key: 'manage', label: 'Manage' }] },
{ module: 'audit',       actions: [{ key: 'view', label: 'View' }] },
{ module: 'entities',    actions: [{ key: 'view', label: 'View' }, { key: 'manage', label: 'Manage' }] },
```

- [ ] **Step 2: Commit**

### Task 3: Wire checkPermission into ~40 routes

**Files:** All route files listed in the permission table from CLAUDE.md.

Each route gets `checkPermission(auth, module, action)` after `getAuthUser()`. Pattern:

```typescript
import { checkPermission } from '@/src/lib/workspace-auth';
// In handler, after auth:
const denied = checkPermission(auth, 'module', 'action');
if (denied) return denied;
```

Routes to wire (grouped by module):

**suppliers:** `suppliers/[id]/route.ts` PATCH+DELETE → `manage`
**bills:** `bills/[id]/route.ts` PATCH→`create`, DELETE→`delete`; `bills/[id]/pay/route.ts` POST→`create`
**customers:** `customers/[id]/route.ts` PATCH+DELETE → `manage`
**expenses:** `expenses/route.ts` POST→`create`; `expenses/[id]/route.ts` PATCH+DELETE→`create`; `expenses/[id]/approve/route.ts` POST→`approve`; `expenses/[id]/reject/route.ts` POST→`approve`; `expense-approval-settings/route.ts` PATCH→`approve`
**quotes:** `quotes/route.ts` POST→`create`; `quotes/[id]/route.ts` PATCH+DELETE→`create`; `quotes/[id]/send/route.ts` POST→`create`; `quotes/[id]/convert/route.ts` POST→`create`
**creditNotes:** `credit-notes/route.ts` POST→`create`; `credit-notes/[id]/route.ts` PATCH→`create`
**journals:** `journals/route.ts` POST→`create`; `journals/[id]/route.ts` DELETE+PATCH→`create`
**po:** `po/route.ts` POST→`create`; `po/[id]/route.ts` PATCH→`create`; `po/[id]/approve/route.ts` POST→`approve`; `po/[id]/reject/route.ts` POST→`approve`; `po/settings/route.ts` PATCH→`approve`; `po/approver-mappings/route.ts` POST→`approve`
**projects:** `projects/route.ts` POST→`manage`; `projects/[id]/route.ts` PATCH→`manage`
**mileage:** `mileage/route.ts` POST→`create`; `mileage/[id]/route.ts` DELETE→`create`; `mileage/[id]/approve/route.ts` POST→`approve`; `mileage/[id]/reject/route.ts` POST→`approve`
**vat:** `hmrc/vat/submit/route.ts` POST→`submit`
**coa:** `accounts/route.ts` POST→`manage`; `accounts/[id]/route.ts` PATCH+DELETE→`manage`
**entities:** `entities/[id]/route.ts` PATCH+DELETE→`manage`
**settings:** `settings/route.ts` PATCH→`view`
**banking:** `banking/[id]/match/route.ts` POST→`reconcile`

- [ ] **Step 1-40: Wire each route** (see route list above)
- [ ] **Step 41: Run MCP tests** to verify owner access still works (48/48)
- [ ] **Step 42: Commit**

---

## Phase 2: Mismatch Flagging (#4)

### Task 4: Database migration

**Files:**
- Create: `database/migrations/028_mismatches.sql`

- [ ] **Step 1: Write migration**

```sql
CREATE TABLE IF NOT EXISTS mismatches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  entity_id UUID NOT NULL,
  type TEXT NOT NULL,
  severity TEXT DEFAULT 'warning',
  source_type TEXT NOT NULL,
  source_id UUID NOT NULL,
  reference_type TEXT NOT NULL,
  reference_id UUID NOT NULL,
  source_amount DECIMAL(12,2),
  reference_amount DECIMAL(12,2),
  difference DECIMAL(12,2),
  message TEXT,
  status TEXT DEFAULT 'open',
  resolved_at TIMESTAMPTZ,
  resolved_by UUID,
  created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_mismatches_user_status ON mismatches(user_id, status);
CREATE INDEX idx_mismatches_entity ON mismatches(entity_id);
```

- [ ] **Step 2: Run migration**
- [ ] **Step 3: Commit**

### Task 5: Mismatch service

**Files:**
- Create: `src/lib/mismatch.service.ts`

Functions: `detectBillPOMismatch`, `detectBankMismatch`, `getMismatches`, `resolveMismatch`, `getMismatchCount`

- [ ] **Step 1: Write service**
- [ ] **Step 2: Commit**

### Task 6: Mismatch API routes

**Files:**
- Create: `app/api/mismatches/route.ts` — GET list
- Create: `app/api/mismatches/[id]/route.ts` — PATCH resolve/ignore

Both tier-gated to `mismatch_flagging`.

- [ ] **Step 1: Write routes**
- [ ] **Step 2: Commit**

### Task 7: Integration hooks

**Files:**
- Modify: `app/api/bills/route.ts` — call `detectBillPOMismatch()` after bill creation if `poId` present
- Modify: `app/api/banking/[id]/match/route.ts` — call `detectBankMismatch()` after manual match

- [ ] **Step 1: Wire integration points**
- [ ] **Step 2: Run MCP tests** (48/48)
- [ ] **Step 3: Commit**

---

## Phase 3: Developer API + Webhooks (#6)

Follows the existing detailed plan at `docs/superpowers/plans/2026-03-28-developer-api.md` (15 tasks).

### Task 8: DB migration (026_developer_api.sql → 029)
### Task 9: API key service
### Task 10: Rate limiter
### Task 11: Webhook service
### Task 12: Middleware — API key auth
### Task 13: API helper — v1-helpers.ts
### Task 14: /api/v1/ route handlers
### Task 15: API key management routes
### Task 16: Webhook cron route
### Task 17: Webhook event hooks in service layer
### Task 18: Settings UI — API Keys panel
### Task 19: Settings UI — Webhooks panel
### Task 20: Settings page — add tabs
### Task 21: Tests
### Task 22: Build and deploy

---

## Verification

After all phases:
- [ ] MCP tests pass (48/48)
- [ ] Docker build succeeds
- [ ] Container starts healthy
